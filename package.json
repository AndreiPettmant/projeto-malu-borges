{
  "name": "projeto-malu-borges",
  "version": "1.0.0",
  "private": true,
  "description": "Aplicativo de gestão de campanhas - Malu Borges",
  "scripts": {
    "dev": "cd frontend && npx vite",
    "dev:netlify": "npx netlify dev",
    "install:all": "cd frontend && npm install",
    "build": "cd frontend && npm run build"
  },
  "devDependencies": {
    "netlify-cli": "^23.15.1"
  }
}
